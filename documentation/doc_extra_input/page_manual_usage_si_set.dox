/*! \page page_manual_usage_si_set set
\code{.cpp}
template<typename T> class set{
	//default constructor
	set();
	//copy constructor
	set(set &in other);
	//operator=
	set& opAssign(set &in other);
	
	void SetDirectcomp(bool yesno);
	
	int size();
	void clear();
	bool empty();
	
	void insert(T &in value);
	void erase(T &in value);
	bool contains(T &in value);
};
\endcode
*/